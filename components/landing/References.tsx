import styled from 'styled-components';

const references = () => <Container>References</Container>;

export default references;

/*
 * Styled declarations
 */
const Container = styled.div`
    grid-area: references;
    background-color: green;
`;
